import React from 'react';

// PL: Komponent strony "Logowanie" (obecnie tylko placeholder).
// EN: "Login" page component (currently just a placeholder).

const LoginPage = () => <h2>Strona logowania</h2>;

export default LoginPage;